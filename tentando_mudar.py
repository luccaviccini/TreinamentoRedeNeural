def main():
    a1 = [1,3]
    a2 = [2,1]
    ordem = int()
    d = float()
    i,j = int()
    
    d = determinant(a1,a2,ordem)
    if (d == 0):
        print("A matriz nao é invertível")
    else:
        (cofator(a1,a2,k)) 
           
def determinant(a1,a2,k):
    s = 1
    det = 0
    b1 = list()
    b2 = list()
    i,j,m,n,c = int()
    ## se a ordem for 1, retornar a[0,0]
    if(ordem == 1):
        return a1[0]
    else:
        det = 0
        while (c<k):
            m = 0
            n = 0
            while (i<k):
                while (j<k):
                    b1[j]
 
    
    
    
